[package]
name = "flea"
version = "0.1.3"
edition = "2021"
# src/vulkan.rs uses c"..." literals, stabilised in 1.77; without this the error names rustc, not flea.
rust-version = "1.77"

[dependencies]

[profile.release]
opt-level = 3
lto = true
strip = true
